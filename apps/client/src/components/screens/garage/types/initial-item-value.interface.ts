export interface IInitialItemValue {
  item: number;
  paint: number | null;
  cert: number | null;
  quality: number | null;
  series: number | null;
  special: number | null;
  blueprint: boolean;
  amount: number;
}
